#Room occupancy manager app. 

Java 16 and latest Spring Boot. 
H2 was used as a storage to make development rapid.

To build project (testing included) use delivered maven wrapper:
`./mvn clean install`

To run application
`./mvnw spring-boot:run`
